"use client";

// ============================================================
// RayzorFlow — application providers
// ============================================================

import { type ReactNode } from "react";
export default function Providers({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
